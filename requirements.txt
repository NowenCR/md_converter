# MD Forge — Dependencies
# Install with: pip install -r requirements.txt

# PDF support (choose one or both; pdfplumber is preferred)
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Word (.docx) support
python-docx>=1.1.0

# RTF support
striprtf>=0.0.26

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ODT (OpenDocument Text) support
odfpy>=1.4.1

# EPUB support
ebooklib>=0.18

# Optional: chardet for better encoding detection
chardet>=5.0.0

# MD Forge — Dependencies
# Install with: pip install -r requirements.txt

# PDF support (choose one or both; pdfplumber is preferred)
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Word (.docx) support
python-docx>=1.1.0

# PowerPoint (.pptx / .ppt) support
python-pptx>=0.6.23

# RTF support
striprtf>=0.0.26

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ODT (OpenDocument Text) support
odfpy>=1.4.1

# EPUB support
ebooklib>=0.18

# Optional: chardet for better encoding detection
chardet>=5.0.0
